# THIS FILE HAS BEEN GENERATED BY CHEF
# ANY MANUAL MODIFICATIONS WILL BE OVERWRITTEN

description "rabbitmq-server"

start on runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 10 5

env DAEMON=/usr/sbin/rabbitmq-server
env LOG=<%= node.rabbitmq.logdir %>/startup_log
env ERR=<%= node.rabbitmq.logdir %>/startup_err

script
  if [ -d <%= node[:rabbitmq][:mnesiadir] %> ]; then
    su - rabbitmq -s /bin/sh -c "$DAEMON > $LOG 2> $ERR"
  fi
end script
